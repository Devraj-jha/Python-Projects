A = {1,2,3}
B = {3,4,5}
print(A | B) # union of set

A & B # intersection

A - B # substraction
A ^ B #Symmetric Difference
s = {1,2,3,4,5}
s.add(10)
s.remove(2)      # error if not present
s.discard(99)    # safe
s.clear()
# clears
# interation is same as list using for in 

# we can use set to remove duplicate from something 
